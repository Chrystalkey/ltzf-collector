# Logging
- logging to file
- logging program errors to file
- logging parsing errors hinting at a change of the source material to a separate file


-> info/stdout log to file
## Errors (Object created as expected or connection errors)
- soft errors (logged / object still created)
    - object sent unsuccessfully
        - forbidden (can happen)
        - connection errors
    - special case 404: Broken links

## Parsing Errors (Object not created due to format errors)
- hard errors (warned / logged / object not created)
    - website parsing errors
        - unknown website elements
        - pdfs did not contain expected data
    - object sent unsuccessfully
        - Bad Request (should not happen)
    - object creation error
        - no text extracted from document

